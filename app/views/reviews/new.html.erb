<%= form_for(@review) do |f|%>
  <%= f.label :comment %>
  <%= f.text_area :comment %>
  <br>
  <%= f.label :rating %>
  <%= f.number_field :rating %>
  <%= f.label :book_id %>
  <%= f.collection_select :book_id, Book.all, :id, :title %>
  <%= f.submit %>
<% end %>
